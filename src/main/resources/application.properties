# --- SERVER PORT ---
server.port=${PORT:8080}

# --- DATENBANK ---
spring.datasource.url=${SPRING_DATASOURCE_URL:jdbc:postgresql://localhost:5432/theater_shoe}
spring.datasource.username=${SPRING_DATASOURCE_USERNAME:shoe_user}
spring.datasource.password=${SPRING_DATASOURCE_PASSWORD:passwort123}
spring.datasource.driver-class-name=org.postgresql.Driver

spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.PostgreSQLDialect

# --- UPLOAD LIMITS ---
spring.servlet.multipart.max-file-size=10MB
spring.servlet.multipart.max-request-size=10MB

# --- SICHERHEIT (Der Teil hat gefehlt!) ---
spring.security.user.name=schuhfee
spring.security.user.password=theater123